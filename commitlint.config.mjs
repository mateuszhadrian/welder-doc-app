/**
 * Conventional Commits (analiza §4.3).
 * Dozwolone typy: feat, fix, chore, docs, refactor, test, style, perf, build, ci.
 */
const config = {
  extends: ['@commitlint/config-conventional']
};

export default config;
